<script>

//  Here we will have logic which will populate the info window with the js.
// initially, it should say 'Find course'
// on result,
// if there are no errors,
// then change info to - search result number.
// if there are errors then do not render the course_table
    <%if 1 > 2 %>
        CHEMCERT.utils.ch_message.add("hello", "done")
    <% else %>
    CHEMCERT.utils.ch_message.add("hello", "error");
    CHEMCERT.utils.ch_message.clear("global-message");
    window.alert('ok');
    CHEMCERT.utils.ch_message.add("hello", "error");
    <% end %>
</script>

<%= render 'course_table' %>

